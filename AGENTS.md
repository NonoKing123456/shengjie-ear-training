# Codex 项目协作规则

## 项目概况

- 这是一个完全在浏览器本地运行的练耳训练应用，当前代码主要位于 `dist/`。
- 应用不依赖后端、账号或云端数据库；训练记录保存在浏览器 Local Storage。
- `docs/` 保存需求和设计文档，`tests/` 保存 Node.js 内置测试。

## 目录约定

- `dist/index.html`：页面结构和入口。
- `dist/styles.css`：样式、响应式布局和交互状态样式。
- `dist/app.js`：训练流程、音频调度、历史记录、报告和页面交互。
- `dist/jazz.js`：传统爵士和声语法、题目生成、干扰项和 voicing 逻辑。
- `dist/jazz-notation.js`：爵士和弦五线谱渲染。
- `dist/piano-sampler.js`：钢琴采样加载和播放。
- `docs/`：需求文档；新增功能先更新对应需求文档，再实现代码。
- `tests/`：可复现的逻辑测试；涉及题目生成、voicing 或谱面渲染时优先补充测试。

## 修改原则

- 修改前先阅读相关模块和对应测试，只改完成任务所需的文件。
- 优先复用现有函数、数据结构、事件处理和视觉样式，不引入新的框架或依赖。
- 保持浏览器原生 JavaScript；不要把 CommonJS 测试代码混入浏览器运行文件。
- 不把账号、密钥、浏览器数据、音频采样或生成的用户历史写入 Git。
- 训练题目必须遵守用户选择的调性、语汇包、题目数量和 voicing 模式；不要用随机拼接替代现有和声规则。
- 音频相关改动要保持离线可运行，并注意浏览器的用户手势限制。
- 修改需求、交互行为或数据格式时，同时更新 `docs/` 中对应文档。

## 本地运行

推荐在项目根目录执行：

```powershell
py -m http.server 4173 -d dist
```

然后访问 `http://127.0.0.1:4173/`。也可以运行 `start.bat`。

不要直接用 `file://` 作为唯一验证方式，因为浏览器可能限制模块、音频或本地资源访问。

## 测试与验证

- 逻辑测试：`node --test tests\*.test.cjs`
- 修改页面或音频后，至少手动打开本地服务，验证入口、相关训练流程和答后复盘。
- 修改题目生成器时，检查题目长度、选项数量、语法合法性、语汇包边界、非重复和 voicing 约束。
- 修改五线谱渲染时，检查实际 MIDI 音符、升降号、谱线和移动端布局。
- 完成任务后查看 `git diff`，确认没有无关文件、调试输出或敏感数据。

## 文档工作流

- 需求文档使用 `docs/` 下已有文档的中文风格。
- 新需求至少写明：背景、目标、范围、非目标、用户流程、功能要求、异常情况和验收标准。
- 实现前先确认需求与现有行为是否冲突；实现后更新验收状态或补充已知限制。

## 汇报格式

完成任务时只需汇报：

1. 根因或实现思路。
2. 修改的文件及其目的。
3. 执行的测试和结果。
4. 仍需人工确认的问题。
